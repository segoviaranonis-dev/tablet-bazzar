# 4.01.07.004 · Report Aprobaciones — «Sin grada» en FI PE

**Código:** `4.01.07.004`  
**App:** Report · `/aprobaciones` · FI PE  
**Detectado:** 2026-07-24 · pedido Enrique `PVR-2026-942359` (luego eliminado)  
**Estado:** 🟡 **PARCIAL** — simulación `PVR-2026-891496` / FI `PE-217-001` con `gradas_fmt` OK; falta fallback UI a `ppd.grada` si snapshot vacío  
**CHUSAR:** `2.2.1.22`

---

## Síntoma

Tarjeta Aprobaciones mostraba **Sin grada** en ítems PE aunque PPD tenía texto `grada` (ej. `34(1 1 2 2 1 1)39`).

---

## Causa

1. Script de confirm (`_cerrar_enrique_10pct.mjs`) envió `gradas_fmt: ''` en `linea_snapshot`.
2. Calzado PE: `grades_json` NULL → parser UI no reconstruye curva.
3. UI Aprobaciones prioriza `gradas_fmt` / `grades_json` y **no cae** a `ppd.grada`.

---

## Mitigación demostrada

Simulación 2026-07-24: payload con `gradas_fmt` desde vista → FI 3501 `sample_grada = 35(1 1 2 2 1 1)40`.

---

## Deuda

- Fallback display: snapshot vacío → `pedido_proveedor_detalle.grada`.
- Scripts confirm: nunca mandar `gradas_fmt` vacío si hay `grada` en stock.

---

## Relacionados

- `linea-snapshot-display.ts` · protocolo imágenes/gradas FI
- Pedido eliminado 216 / reintegro 191 pares (orden Director)
