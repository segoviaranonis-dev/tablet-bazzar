# 4.02.02.002 · Report — `.next` corrupto (chunk / routes-manifest)

**Capa:** Report · Next.js 15 dev local  
**Estado:** ✅ Hotfix conocido  
**Keyword:** **Bug urgente!!**

## Síntoma

```
Cannot find module './1331.js'
ENOENT routes-manifest.json
GET /ventas-fotos 500
```

Tras `next dev` + `next build` en paralelo, o reinicio sin borrar `.next`.

## Fix (PowerShell — `report/`)

1. Matar proceso en puerto **3001**
2. Borrar `.next` y `node_modules/.cache`
3. **Solo** `npm run dev:clean:3001` — **no** `npm run build` con dev vivo
4. Verificar terminal: `Ready in` + ruta afectada sin `MODULE_NOT_FOUND`

## Regla agente

No declarar tarea terminada sin leer la terminal del comando (ver `PROTOCOLO_5_PATAS.md` Pata 5 · `Terminal:` en 💰 COSTO).

---
*Índice: `4.02.02.002` · `.claude/5_errores/INDICE_ERRORES.md`*
