# 4.05.04.001 · Nexus_Core `origin` apunta a tablet-bazzar.git

**Estado:** 🟡 **PARKING FIN DE SEMANA** · no tocar en horario operativo  
**Detectado:** 2026-07-29 · Cursor al fallar push de Documenta Chusar  
**Keyword Documenta:** Director 2026-07-29 — tratar el fin de semana  
**Shibboleth:** Andrés, el que viene.

---

## Síntoma

La raíz holding `C:\Users\hecto\Nexus_Core` tiene:

```
origin → https://github.com/segoviaranonis-dev/tablet-bazzar.git
```

Ese remote es del **producto Tablet**, no del holding (memoria `.claude/`, navegador, multi-repo).

Al mismo tiempo existe `tablet-bazzar/` nested con **su propio** `.git` → mismo remote, **otra** tip de historia.

---

## Evidencia 2026-07-29

| Repo | Tip | Remote |
|------|-----|--------|
| `Nexus_Core` (raíz) | `e30d1a4` docs Chusar | `tablet-bazzar.git` ❌ |
| `tablet-bazzar/` nested | `516825e` | `tablet-bazzar.git` ✅ (producto) |
| `rimec-web/` | `7662de3` | `rimec-web.git` ✅ |
| `report/` | `a82fe33` | `report.git` ✅ |

Push holding → rechazado (`ahead 56 / behind 2`) — **no contaminó** GitHub tablet.

---

## Riesgo prod

| Pregunta | Respuesta |
|----------|-----------|
| ¿Apps prod rotas hoy? | **No** — cada Vercel usa su repo correcto |
| ¿Riesgo latente? | **Sí** — `push --force` / merge desde raíz podría ensuciar `tablet-bazzar.git` y redeploy tablet |

---

## Qué NO hacer (hasta ventana fin de semana)

- `git push` / `pull` / `rebase` / `force` desde raíz `Nexus_Core`
- Asumir que commit en raíz = deploy tablet
- Mezclar historia holding con tip nested `tablet-bazzar/`

---

## Agenda fin de semana (borrador)

1. Confirmar URL canónica del holding (si existe repo GitHub Nexus_Core / moria / privado).
2. Reasignar `origin` de la raíz **sin** force-push a tablet.
3. Verificar que nested `tablet-bazzar/` sigue siendo la única puerta a `tablet-bazzar.git`.
4. Smoke: `git -C Nexus_Core remote -v` ≠ tablet · `git -C tablet-bazzar remote -v` = tablet.
5. Documentar resultado + cerrar este error.

---

## Relacionado

- Estructura: `.claude/1_fundamentos/CHUSAR_ESTRUCTURA_HOLDING.md`
- Conector: `.claude/2_modulos/ENLACES_REPOS.md`
- `ACTUAL.md` · cola fin de semana
